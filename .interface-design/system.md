# Legacy pointer

The canonical UI contract is now [`docs/ui.md`](../docs/ui.md). Read and update
that file for current design rules. This filename remains only as a migration
pointer for older plans and links.
